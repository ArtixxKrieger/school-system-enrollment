{
  "buildCommand": "pnpm install && pnpm --filter @workspace/scripts run migrate && pnpm --filter @workspace/scripts run seed && pnpm --filter @workspace/enrollment run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/handler" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
