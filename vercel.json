{
  "buildCommand": "pnpm install && (pnpm --filter @workspace/db run push || echo 'Schema push skipped') && (pnpm --filter @workspace/scripts run seed || echo 'Seed skipped') && pnpm --filter @workspace/enrollment run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/handler" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
