{
  "version": 2,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/enrollment run build",
  "outputDirectory": "artifacts/enrollment/dist/public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" }
  ]
}
