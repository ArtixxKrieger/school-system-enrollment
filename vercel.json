{
  "outputDirectory": "artifacts/enrollment/dist",
  "buildCommand": "pnpm install && pnpm --filter @workspace/api-spec run codegen && pnpm --filter @workspace/enrollment run build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
